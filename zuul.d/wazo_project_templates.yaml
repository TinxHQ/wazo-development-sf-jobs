---
- project-template:
    name: yarn-test
    description: Project template for yarn test
    check:
      jobs:
        - yarn-test
    gate:
      jobs:
        - yarn-test
    auto-merge:
      jobs:
        - noop
